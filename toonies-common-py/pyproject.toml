[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "toonies-common"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "msgpack>=1.1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/toonies_common"]
